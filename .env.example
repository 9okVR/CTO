# CryptoTracker Environment Variables
# Copy this file to `.env.local` or `.env` and update values as needed.

# Base URL for the CoinGecko API. Override this if you proxy requests or use a paid API endpoint.
COINGECKO_API_BASE=https://api.coingecko.com/api/v3

# Interval (in milliseconds) between market data refreshes.
REFRESH_INTERVAL_MS=60000

# CoinGecko API key for paid plans (optional). Leave blank for the free public API.
# COINGECKO_API_KEY=
